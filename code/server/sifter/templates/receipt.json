{
  "id": "receipt",
  "name": "Receipt",
  "description": "Retail receipts — merchant, items purchased, totals, payment method.",
  "icon": "receipt",
  "instructions": "Extract: merchant name, merchant address, receipt date, receipt time, items (description + quantity + unit price), subtotal, tax amount, total amount, payment method, last 4 digits of card (if present). Mark fields not present in the document as null.",
  "schema_fields": [
    {"name": "merchant_name", "type": "string", "nullable": true},
    {"name": "merchant_address", "type": "string", "nullable": true},
    {"name": "receipt_date", "type": "date", "nullable": true},
    {"name": "receipt_time", "type": "string", "nullable": true},
    {"name": "subtotal", "type": "number", "nullable": true},
    {"name": "tax_amount", "type": "number", "nullable": true},
    {"name": "total_amount", "type": "number", "nullable": true},
    {"name": "payment_method", "type": "string", "nullable": true},
    {"name": "card_last_four", "type": "string", "nullable": true}
  ]
}
